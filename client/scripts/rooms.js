var Room = function(name, password) {
  this.name = name;
  this.password = password;
  this.storage = [];
  // how many people are allowed into the room?
  // how many messages are shown by default?
};

var Rooms = {


};